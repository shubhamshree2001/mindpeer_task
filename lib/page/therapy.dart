import 'package:flutter/material.dart';

class Therapy extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('therapy page'),
    );
  }
}
