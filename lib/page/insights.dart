import 'package:flutter/material.dart';

class Insights extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('insight page'),
    );
  }
}
