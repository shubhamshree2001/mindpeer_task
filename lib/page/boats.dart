import 'package:flutter/material.dart';

class Boats extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('boat page'),
    );
  }
}
